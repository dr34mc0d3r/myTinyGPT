Create new clone of myTinyGPT
from desired development path:

git clone git@github.com:dr34mc0d3r/myTinyGPT.git
cd myTinyGPT

This project includes 'ai_system/pyproject.toml' so run:
uv venv

now you are ready to proceed.
